# Guard symbol

A scoped name declared in the net schema identifying a guard required
by a transition, mapped to a concrete guard function before the
process can run.

- Detail: [spec/handler-contract.md](../../../spec/handler-contract.md)
