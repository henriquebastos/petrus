# Quiescent

The state of a net instance in which no firing candidate is enabled,
no firing occurrence has begun without ending, and no firing binding
has a derived future timer maturation. Quiescence triggers deriving
instance status but is never the verdict itself.
