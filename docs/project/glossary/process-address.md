# Process address

The stable identity of one independently authoritative net instance as
a communication endpoint. It never names the instance's current
worker, runner, provider session, or execution location.
