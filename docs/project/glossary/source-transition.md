# Source transition

A transition with no input arcs — the entry point for external events.
Source-ness is a structural property, not an implementation kind; a
source transition never auto-fires, firing only on external delivery.

- Detail: [spec/net-schema.md](../../../spec/net-schema.md)
