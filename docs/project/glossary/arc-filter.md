# Arc filter

An optional pure, single-token boolean on an input arc — inline CEL or
a named pure symbol — selecting which tokens of the declared color the
arc admits. A filter never transforms token data and never spans
tokens.

- Related: [Guard](guard.md)
- Detail: [spec/net-schema.md](../../../spec/net-schema.md)
