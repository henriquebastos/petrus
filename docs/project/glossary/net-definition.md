# Net definition

A declarative description of process topology and coordination
semantics: places, transitions, arcs, token/color types, guards,
subnet boundaries, and transition contracts. It contains no concrete
external behavior; handlers bind to transitions rather than deriving
the net from handler order.

Related: [Handler binding](handler-binding.md)
