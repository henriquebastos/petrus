# Sensor

An ingress adapter that offers identified deliveries to armed source
transitions as a prompt, nonblocking observation of already-available
process-local ingress. Blocking pull work is modeled as an Activity;
Sensors are live-side code re-supplied at resume, never replayed.

- Detail: [spec/net-schema.md](../../../spec/net-schema.md)
