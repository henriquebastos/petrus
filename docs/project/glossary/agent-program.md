# Agent Program

The structure and policy that progresses agent work, owned by an
official provider runtime, an application harness, or an Impetus Net —
distinct profiles rather than implementations of one universal Agent.
A program is not its model, connection, live runtime, Thread, or
Hands.
