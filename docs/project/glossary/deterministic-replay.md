# Deterministic replay

The property that, given the same external events, activity lifecycle
facts, and timer events in the same order, a net instance reaches the
same state.

- Detail: [spec/firing-semantics.md](../../../spec/firing-semantics.md)
