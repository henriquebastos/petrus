# History Store

The capability that preserves one Instance's History through the
structural `HistoryStore` append/read contract. Product profiles —
In-Memory, Local, and Transactional History Store — name failure and
transaction boundaries; memory, JSONL, SQLite, and PostgreSQL are
implementation substrates, not capability names.
