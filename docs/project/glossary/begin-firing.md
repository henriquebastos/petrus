# Begin firing

The runtime step that starts a firing occurrence, records it durably,
and consumes or accounts for input tokens according to the net
semantics before delegating to the handler.

- Detail: [spec/firing-semantics.md](../../../spec/firing-semantics.md)
