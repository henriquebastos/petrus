# Activity queue routing

Operational Engine configuration consisting of one default queue plus
optional exact per-Activity queue overrides, providing load balancing
and isolation without entering `ActivityInvocation` or canonical
History.
