# Continuation

Provider- or program-specific state that allows compatible runtime
work to continue a Thread. Agenticus preserves explicit compatibility
and opaque custody where needed, without defining one cross-provider
Continuation format.
