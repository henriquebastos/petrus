# Declaration

An addressable schema element attached to a net or graph citizen that
is not itself a node — handler, guard, arc-filter, timer, and initial
marking declarations. The composed/flattened net assigns every
declaration a deterministic canonical `NetUri`.
