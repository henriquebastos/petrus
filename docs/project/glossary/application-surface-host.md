# Application surface host

An optional outer composition layer that supplies HTTP, SSE, CLI,
browser, authentication, projection, and attach surfaces around one or
more Instances. It is distinct from both Instance coordination and
Fabric.
