# Petrinet Kernel

The semantic center exposed at `petrus.impetus.petrinet`: `Net`,
tokens and markings, enabledness, timer-maturation calculation, and
pure token routing. Candidate Selection, activities, bindings, History
and History Store, dispatch, workers, and hosts are outside it.

- Avoid: Impetus Kernel, Petri-net core
