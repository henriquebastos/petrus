# Token queue

The tokens at one place in FIFO order, each paired with its entry
instant — duplicates are legal and order is significant. The marking
is its time-blind view; the entry instants are its time view.

- Detail: [spec/net-schema.md](../../../spec/net-schema.md)
