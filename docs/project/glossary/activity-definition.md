# Activity definition

A typed, Petri-agnostic Activity implementation produced by
`@activity`, carrying the callable's resolved annotations, its
`ActivityDeclaration`, and the payload converter used around
operational execution. It does not know about nets, arcs, tokens,
History, or Dispatch transport encoding.
