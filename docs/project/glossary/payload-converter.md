# Payload converter

The structural operational seam that decodes one canonical Activity
parameter value into the Python type named by its annotation and
encodes the typed return value back into a detached JSON-faithful
value. Provider wire encoding remains Dispatch-owned.
