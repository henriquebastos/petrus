# Handler result

The current synchronous API envelope containing token and
delivery-registration effects returned by a handler — shipped
compatibility vocabulary, not a separate worker protocol or durable
lifecycle.

- Detail: [spec/handler-contract.md](../../../spec/handler-contract.md)
