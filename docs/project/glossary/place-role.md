# Place role

An optional display/tooling annotation on a place: `input`, `output`,
`control`, or `regular` (the default). Place roles never change
Petri-net enabledness or firing semantics.

- Detail: [spec/net-schema.md](../../../spec/net-schema.md)
