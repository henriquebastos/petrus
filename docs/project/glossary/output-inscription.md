# Output inscription

The inscription on an output arc: a routing contract naming the token
color/type the arc admits into its target place. It is not a token
factory — the handler supplies output tokens and the runtime deposits
only those whose color and destination match an output arc.
