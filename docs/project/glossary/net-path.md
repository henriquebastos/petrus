# Net path

A first-class static address object for a place or transition node
inside a hierarchical net definition. Places and transitions share the
same node namespace within each parent scope, so a net path identifies
exactly one node.

Related: [Net URI](net-uri.md),
[Relative net path](relative-net-path.md)
