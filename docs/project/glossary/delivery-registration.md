# Delivery registration

A binding/runtime-layer record that the runtime may still deliver an
external event to a source transition of a net instance, identified by
its source transition and a caller-chosen key. Open and close are
recorded process facts; the serialized family is
`DeliveryRegistration` / `DeliveryRegistrationOpened` /
`DeliveryRegistrationClosed`.
