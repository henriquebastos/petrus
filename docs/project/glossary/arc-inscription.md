# Arc inscription

The typed expression on an arc describing token color/type and
cardinality, plus an optional arc filter. Inscriptions stay simple:
they do not bind handler argument names, and cross-token correlation
belongs in transition guards.
