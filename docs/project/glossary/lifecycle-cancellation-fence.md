# Lifecycle cancellation fence

The recoverable Dispatch instruction projected from an already
committed `ScopeClosed` or `ScopeReset` record for one exact Activity
invocation. It is not hard interruption and never proves an ambiguous
external effect did not occur.
